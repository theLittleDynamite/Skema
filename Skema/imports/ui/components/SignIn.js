import React, { Component } from 'react';

// The sign in button for an existing user
export default class SignIn extends Component {
	render() {
		return (
			<button className="SignIn">
			</button>
		);
	}
}